apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-deployment.fullname" . }}
  labels:
    {{- include "helm-chart-deployment.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-deployment.selectorLabels" . | nindent 4 }}
